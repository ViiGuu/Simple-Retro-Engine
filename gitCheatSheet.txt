pushing:

    git push <remote (like origin)> <branch>

create new branch:

    git checkout -b new-branch-name

merging branch with main:

    git checkout main

    git merge other-branch

    git add .

    git commit -m "message"

    git push origin main

